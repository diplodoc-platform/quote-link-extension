/**
 * Quote link extension: MarkdownIt plugin and types for blockquotes with `data-quotelink` links.
 * @module
 */

export type {TransformOptions} from './transform';
export {transform} from './transform';
export {TokenType} from './const';
